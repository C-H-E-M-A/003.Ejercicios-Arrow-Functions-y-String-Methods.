/* Crea una función que convierta una cadena de texto en mayúsculas usando el
método toUpperCase().
Ejemplo: "Hola" → "HOLA"*/

let cadena="Unicornio";

const MAYUSCULA=(cadena)=>{
    let nueva=cadena.toUpperCase();
    console.log(nueva);
}
MAYUSCULA(cadena);