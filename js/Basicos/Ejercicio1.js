/*Escribe una función que reciba una cadena de texto y devuelva su longitud.
Ejemplo: "Hola" → 4 */

let cadena="Unicornio";

const tamano=(cadena)=>{
    let nueva=cadena.length;
    console.log(nueva);
}
console.log(cadena+"->")
tamano(cadena);
