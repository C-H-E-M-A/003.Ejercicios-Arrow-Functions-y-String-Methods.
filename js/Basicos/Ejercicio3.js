/*Escribe una función que convierta una cadena a minúsculas utilizando
toLowerCase().
Ejemplo: "MUNDO" → "mundo"*/

let cadena="Unicornio";

const minúscula=(cadena)=>{
    let nueva=cadena.toLowerCase();
    console.log(nueva);
}
minúscula(cadena);